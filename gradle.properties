# JVM Memory Settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Gradle Configuration

# Enable parallel build
org.gradle.parallel=true

# Configure build scans
org.gradle.buildScan.publish.showLink=false